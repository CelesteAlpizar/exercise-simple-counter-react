// // Global fontAwesomeConfig

// // 1) First you have to import the #fortawesome/fontawesome library

// import fontawesome from "fortawesome/fontawesome";

// // 2) Then you have to import every icon that you will use (when the icon say far Fa... the r of the far means regular and if say fas Fa... the s of the fas means solid, we also can see in the end of the URL if it say regular or solid)

// import faClock from "fortawesome/fontawesome-free-regular/faClock";

// // 3) Tell font-awesome that you want to replace your icons with SVGs (recomended for performance)

// fontawesome.config = {
// 	autoReplaceSvg: "nest"
// };

// // // 4) Add the icons into the font-awesome library

// fontawesome.library.add(faClock);

// // // 5) Place the icon tag anywhere you want to show instanceof. Ex: <i class="fas fa-check"></i>

// // // 6)Always remember make the reference in the index that you're working to import the document where you make this process

// // import "./icons.js";
